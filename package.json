{
  "name": "s",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build:p": "node scripts/build-p.js",
    "build:legacy": "node scripts/legacy-obfuscate-html.js",
    "typecheck": "tsc --noEmit",
    "build": "node scripts/build-p.js && node scripts/legacy-obfuscate-html.js"
  },
  "devDependencies": {
    "cheerio": "^1.0.0",
    "clean-css": "^5.3.3",
    "esbuild": "^0.24.0",
    "html-minifier-terser": "^7.2.0",
    "javascript-obfuscator": "^5.5.0",
    "typescript": "^5.6.0"
  }
}
